numpy>=2.0

# 사이드카 HTTP 서버 (sidecar/server.py)
fastapi>=0.100
uvicorn>=0.23

# ingest 파이프라인 (영상/오디오/PDF/텍스트 → 개념 그래프)
ollama>=0.4          # 로컬 LLM 추출 (format=schema 제약 디코딩)
pydantic>=2.0        # 추출 스키마
faster-whisper>=1.0  # 영상·오디오 → 자막 STT (시스템 ffmpeg 필요)
pypdf>=4.0           # PDF 텍스트 추출 (디지털 PDF; 스캔본은 OCR 별도)
# reportlab          # (테스트 전용) verify_loaders.py 의 한국어 PDF 생성

# 운영 단계 숙달도 추적용 (콜드스타트/검증엔 불필요 — reference §6-6):
# pyBKT>=1.4
